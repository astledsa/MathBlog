 baseURL = "/"
languageCode = "en-us"
title = "mathblog"
copyright = "whatever."

rssLimit = 20
staticDir = "static"
canonifyURLs = true

[taxonomies]
  category = 'categories'
  tag = 'tags'
  
[author]
	name = "Astle"
	email = "astledsa@gmail.com"

[params]
  description = "Math, something I love and hate"

[markup.goldmark.renderer]
    unsafe = "true"

[frontmatter]
  date = ['date', 'publishdate', 'pubdate', 'published', 'lastmod', 'modified']
  expiryDate = ['expirydate', 'unpublishdate']
  lastmod = [':git', 'lastmod', 'modified', 'date', 'publishdate', 'pubdate', 'published']
  publishDate = ['publishdate', 'pubdate', 'published', 'date']